{
  "name": "quotes-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cy:run-live": "npx cypress run --config baseUrl=$GH_PAGES_URL"
  },
  "devDependencies": {
    "cypress": "^15.3.0"
  }
}
