{
  "name": "quotes-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server . -p 8080",
    "cy:run-local": "npx wait-on http://localhost:8080 && npx cypress run --config baseUrl=http://localhost:8080",
    "cy:run-live": "npx cypress run --config baseUrl=$GH_PAGES_URL"
  },
  "devDependencies": {
    "cypress": "^15.3.0",
    "http-server": "^14.1.1",
    "wait-on": "^7.0.1"
  }
}
